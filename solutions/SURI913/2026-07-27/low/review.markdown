#### 시간복잡도 `O(NlogN)`
>
>   Checkpoint: 박수를 치는 부분은 숫자 대신, 박수 횟수에 맞게 “-“ 를 출력한다.
>   여기서 주의해야 할 것은 박수 한 번 칠 때는 - 이며,
>   박수를 두 번 칠 때는 - - 가 아닌 -- 이다. 
>
>   ex) 31의 경우 -, 33의 경우 --
>
>   입력값의 **마지막 자리수만(num % 10)** 떼내서 3,6,9에 해당하는지 체크.

배열을 사용하면 O(N)도 가능 => DP스타일